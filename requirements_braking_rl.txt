# Optional, isolated offline training only. Never install into the flight venv.
# Exact versions used by the 2026-09-03 macOS arm64 experiment.
stable-baselines3==2.7.0
gymnasium==1.2.3
torch==2.14.0
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
